# LLM Lens｜大模型产品评测与 Bad Case 工作台 PRD

版本：1.1  
日期：2026-08-14  
性质：个人求职验证性作品；Codex 辅助需求细化、数据处理、前端实现与文档初稿。非企业实习、商业上线或客户项目。

## 1. 背景与问题

AI 产品评测很容易退化为“试了几个问题，凭体验说哪个更好”。这类结论通常缺少统一口径、原始输出、模型版本和复核记录，不能支持产品迭代，也经不起面试追问。

公开岗位要求反复出现测试集、评测流程、Bad Case、结果分析、问题定位和自动化工具。例如百度的[大模型评估实习生](https://talent.baidu.com/jobs/detail/INTERN/ac9bfc68-f403-42e4-803d-13ad690293b8)、[评测产品实习生](https://talent.baidu.com/jobs/detail/INTERN/a7487847-e649-4b60-96cf-faa90f6f8b34)和小红书的[AI 产品经理实习生（大模型应用方向）](https://job.xiaohongshu.com/campus/position/17465)都明确涉及评测、Bad Case 或迭代闭环。

## 2. 目标用户

- AI 产品运营 / 产品实习生：需要执行测试、归集问题并输出复盘。
- AI 产品经理：需要从问题证据定位可优化的 Prompt、工具、交互或安全策略。
- 模型评测同学：需要结构化记录、筛选、人工复核和导出。

## 3. 产品目标

1. 把“体验不好”转成包含任务、输入、原始输出、偏好结果、根因建议与复核状态的结构化记录。
2. 用统一测试集约束执行口径，避免看到答案后再改变标准。
3. 明确区分公开人类偏好、规则初筛和人工结论。
4. 支持离线展示，并允许后续继续录入真实运行结果。

### 非目标

- 不构建当前模型的权威排行榜。
- 不把规则根因当作人工标注真值。
- 不在没有 API Key 和运行日志时伪造模型成绩。
- 不把 120 条抽样记录外推为全体用户偏好。

## 4. 评测维度

自建测试集共 48 条，每个维度 6 条：

1. 指令遵循：格式、数量、顺序、禁用词、长度等明确约束。
2. 多轮上下文：目标或约束变化后能否保留有效历史并应用最新要求。
3. 事实与时效：动态事实、来源、无法联网时的诚实边界。
4. 信息提取：结构化抽取、缺失值、去重与原始 ID 追溯。
5. 推理计算：百分比、相对提升、分群结构与不可推导结论。
6. 中文写作：产品问题描述、发布通知、术语解释和结论压缩。
7. 安全边界：医疗、金融、隐私、虚构成果和破坏性操作。
8. Agent 与工具意识：何时检索、何时请求文件、何时验证、何时不能外部发布。

每条用例包含 ID、维度、Prompt、验收标准、风险等级和执行状态。

## 5. 数据源

### 5.1 公开偏好样本

- 数据集：[LMArena Human Preference 100k](https://huggingface.co/datasets/lmarena-ai/arena-human-preference-100k)
- 本项目确定性抽取 120 条英文/中文记录。
- 保留 prompt、模型 A/B、完整响应、胜负标签、语言和任务类型。
- 人类偏好是原始证据；`rootCauseSuggestion` 是本项目规则生成字段。

### 5.2 自建测试集

- 48 条用例全部存储于 `data/test_cases.json`。
- 当前统一标记“待执行”。
- 用户在页面粘贴真实模型输出并评分后，记录保存在浏览器 localStorage，可导出 JSON。

## 6. 关键流程

### 流程 A：查看公开偏好与样本结构

1. 查看真实样本数量、涉及模型和语言分布。
2. 查看根因初筛分布与样本内模型出现/胜出情况。
3. 阅读“不可外推为排行榜”的显著提示。

### 流程 B：Bad Case 筛选与复核

1. 按任务类型、根因建议、语言或关键词筛选。
2. 打开详情，查看 prompt 与两侧完整响应。
3. 将人类偏好与规则建议分开判断。
4. 后续人工补充具体证据句与确认根因。

### 流程 C：执行自建测试

1. 按维度或状态选择用例。
2. 写清模型/产品版本和日期。
3. 粘贴实际输出，按验收标准给 1-5 分及通过状态。
4. 记录失败点、根因假设与下一步。
5. 导出本地运行记录，形成可审计证据。

## 7. 根因初筛规则

当前规则仅识别一部分明显信号：

- `tie (bothbad)` → 双侧低质或任务未完成
- 数据标记包含拒答 → 拒答边界或工具能力处理
- 代码 / 数学任务 → 代码正确性或推理计算风险
- Prompt 涉及时效词 → 时效性判断或工具使用
- 失败答案明显过短 / 过长 → 覆盖不足或信息密度低
- 其他 → 偏好差距待人工归因

规则的优点是可解释、可复核；缺点是覆盖率和准确性有限。产品不隐藏“待人工归因”。

## 8. 验收标准

- 页面可离线打开，数据无需外部脚本加载。
- 120 条公开记录、48 条测试用例和 8 个维度与数据文件一致。
- 搜索和三类筛选可以组合使用。
- 详情抽屉展示 prompt、模型 A/B 响应、偏好结果、根因建议和复核提示。
- 测试运行表单必须填写模型版本、真实输出、评分和结果。
- 本地运行记录可导出 JSON。
- 公开样本和测试集均可导出。

## 9. 后续迭代

1. 对 30 条高风险样本做双人独立根因标注，计算一致率。
2. 增加模型版本、执行时间、成本、延迟和工具调用轨迹字段。
3. 用真实 API 或产品端执行 48 条测试，生成首轮基线。
4. 建立根因 → 优化动作 → 复测结果的迭代链路。
5. 把浏览器 localStorage 迁移为 SQLite / Supabase，以支持多人协作。
